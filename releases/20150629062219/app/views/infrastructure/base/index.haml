= render_notice
%h3.pull-left= t "infrastructure.#{controller_name.classify.pluralize.underscore}.index.title"
%span.top-panel-actions
  - if can? :create, controller_name.classify.constantize
    = link_to url_for(action: :new), class: 'btn btn-primary' do
      %i.icon-plus.icon-white
      = t 'buttons.add'

= render 'list', collection: collection
